<VirtualHost *:80>
    ServerName <%=domain%>
    ServerAdmin webmaster@localhost

    DocumentRoot /usr/share/roundcube

    <Directory />
        Order allow,deny
        allow from all
    </Directory>
